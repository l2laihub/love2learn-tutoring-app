# =============================================================================
# Love2Learn Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values
#
# IMPORTANT: Never commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Required)
# -----------------------------------------------------------------------------
# Get these values from your Supabase project:
# 1. Go to https://app.supabase.com
# 2. Select your project (or create one)
# 3. Go to Settings > API
# 4. Copy "Project URL" and the appropriate keys
#
# Keys available:
# - anon (public): Safe to use in client-side code, respects RLS
# - service_role (secret): Bypasses RLS, use only server-side
#
# Example:
# EXPO_PUBLIC_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
# EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

EXPO_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-anon-public-key-here

# Secret key - Only use server-side (Edge Functions, API routes)
# WARNING: Never expose this in client-side code!
SUPABASE_SECRET_KEY=your-service-role-key-here

# -----------------------------------------------------------------------------
# OPENAI CONFIGURATION (Optional - for AI worksheet generation)
# -----------------------------------------------------------------------------
# Get your API key from https://platform.openai.com/api-keys
#
# This is only needed if you want to use the AI worksheet generation feature.
# The app will work without it, but worksheet generation will be disabled.

EXPO_PUBLIC_OPENAI_API_KEY=sk-your-openai-key-here

# -----------------------------------------------------------------------------
# STRIPE CONFIGURATION (Required for subscription features)
# -----------------------------------------------------------------------------
# Get these values from your Stripe Dashboard: https://dashboard.stripe.com
#
# 1. Publishable Key (client-side):
#    Dashboard > Developers > API Keys > Publishable key
#
# 2. Secret Key (server-side only - use in Edge Functions):
#    Dashboard > Developers > API Keys > Secret key
#    WARNING: Never expose this in client-side code!
#
# 3. Webhook Secret:
#    Dashboard > Developers > Webhooks > Your webhook endpoint > Signing secret
#
# 4. Price IDs:
#    Dashboard > Products > Select product > Pricing > Copy price ID
#    Create two products: "Solo" (for individual tutors) and "Pro" (for businesses)

EXPO_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_your-publishable-key-here

# Server-side only (set in Supabase Edge Function secrets)
STRIPE_SECRET_KEY=sk_test_your-secret-key-here
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret-here
STRIPE_SOLO_PRICE_ID=price_your-solo-price-id
STRIPE_PRO_PRICE_ID=price_your-pro-price-id

# -----------------------------------------------------------------------------
# DEVELOPMENT SETTINGS (Optional)
# -----------------------------------------------------------------------------
# Enable debug mode for additional logging
# EXPO_PUBLIC_DEBUG_MODE=true

# Skip authentication for development (shows main app directly)
# EXPO_PUBLIC_SKIP_AUTH=true
